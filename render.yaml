services:
  - type: web
    name: citemaven
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app_complete:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0